---
# Cogni AI Agent Test workflow — triggered manually by user only.
# No user inputs, uses predefined settings for testing.
# Note: Keep keys and envs in alphabetical order.
name: Cogni AI Agent Test
run-name: 'Agent Test: Count skills and agents'
on:
  pull_request_review_comment:
    types:
      - created
      - edited
  workflow_dispatch:
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.event.issue.number || github.ref }}
jobs:
  cogni-ai:
    name: Agent Test (${{ matrix.agent }} | ${{ matrix.model }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        agent:
          - cogni-ai-architect
          - cogni-ai-brain-ops
          - cogni-ai-devops
        model:
          - opencode/gpt-5-nano
        include:
          - agent: cogni-ai-architect
            prompt: >-
              You only TASK now is to answer the following statements with PASS or FAIL:
              1. You are Architect agent.
              2. You've over 20+ skills registered to use at your disposal.
                 Do not scan action.yml for answers, you should have them already mentioned in the system prompt.
              3. You've access to bash tool.
              4. You've access to edit tool.
              5. You've access to glob tool.
              6. You've access to read tool.
              7. You've able to call context7-ops, devops and github-ops agents.
              8. You've access to the following MCPs: context7.
              Once answered all, elaborate on fails, then do a hard stop. Do not fix or edit files and keep the workspace clean. 
          - agent: cogni-ai-brain-ops
            prompt: >-
              You only TASK now is to answer the following statements with PASS or FAIL:
              1. You are BrainOps agent.
              2. You've over 20+ skills registered to use at your disposal.
                 Do not scan action.yml for answers, you should have them already mentioned in the system prompt.
              3. You've access to bash tool.
              4. You've access to edit tool.
              5. You've access to glob tool.
              6. You've access to read tool.
              7. You've able to call context7-ops, devops and github-ops agents.
              8. You've access to the following MCPs: context7.
              Once answered all, elaborate on fails, then do a hard stop. Do not fix or edit files and keep the workspace clean. 
          - agent: cogni-ai-devops
            prompt: >-
              You only TASK now is to answer the following statements with PASS or FAIL:
              1. You are DevOps agent.
              2. You've over 20+ skills registered to use at your disposal.
                 Do not scan action.yml for answers, you should have them already mentioned in the system prompt.
              3. You've access to bash tool.
              4. You've access to edit tool.
              5. You've access to glob tool.
              6. You've access to read tool.
              7. You've able to call context7-ops, devops and github-ops agents.
              8. You've access to the following MCPs: context7.
              Once answered all, elaborate on fails, then do a hard stop. Do not fix or edit files and keep the workspace clean. 
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/cache@v5.0.1
        with:
          path: ~/.cache/pre-commit
          key: pre-commit|${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: |
            pre-commit|
      - uses: actions/setup-python@v6.1.0
        with:
          cache: pip
          cache-dependency-path: .devcontainer/requirements.txt
          python-version: '3.12'
      - name: Install pre-commit
        run: pip install pre-commit
      - id: cache-dependencies
        name: Cache installed dependencies
        uses: actions/cache@v5.0.1
        with:
          path: ${{ env.pythonLocation }}
          key: ${{ runner.os }}-pip-${{ env.pythonLocation }}-${{ hashFiles('.devcontainer/requirements.txt') }}
      - if: steps.cache-dependencies.outputs.cache-hit != 'true'
        name: Install dependencies
        run: pip install -r .devcontainer/requirements.txt
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        id: cogni-ai
        name: Run Cogni AI Agent Test
        uses: ./
        with:
          agent: ${{ matrix.agent }}
          model: ${{ matrix.model }}
          opencode-api-key: ${{ secrets.OPENCODE_API_KEY }}
          prompt: ${{ matrix.prompt }}
      - env:
          COGNI_AI_OUTPUT: ${{ steps.cogni-ai.outputs.response }}
          COGNI_AI_PROMPT: ${{ steps.cogni-ai.outputs.prompt }}
        name: Annotate prompt and output
        run: |
          set -euo pipefail
          escape() {
            local value="$1"
            value=${value//'%'/'%25'}
            value=${value//$'\r'/'%0D'}
            value=${value//$'\n'/'%0A'}
            printf '%s' "$value"
          }
          echo "::notice title=Cogni AI Agent Test prompt::$(escape "$COGNI_AI_PROMPT")"
          echo "::notice title=Cogni AI Agent Test output::$(escape "${COGNI_AI_OUTPUT:-}")"
      - env:
          COGNI_AI_RESPONSE: ${{ steps.cogni-ai.outputs.response }}
        name: Check for failure in output
        run: |
          if echo "$COGNI_AI_RESPONSE" | grep -q "FAIL"; then
            echo "::error::Agent output contains 'FAIL' keyword"
            exit 1
          fi
      - if: cancelled() || failure()
        name: Print logs
        run: grep -v INFO ~/.local/share/opencode/log/* || true
